task: proofsearch

problemset: natural-number-game

max_problems: 500
accumulate_library: false

agent:
    max_mcts_nodes: 500

defaults:
    - _self_
    - agent: mcts

job:
    wandb_project: peano

hydra:
    job:
        chdir: true
